This project requiers a large number of training images, which i didn't upload because of size. Algorithm is trained to recorgnize deodorant bottles in images or videos. In order to train it to recorgnize some
other objects, user would need to import around 350-400 images of that object at least.
